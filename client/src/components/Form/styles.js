import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
  labelRoot: {
    right: "20px",
  },
  shrink: {
    transformOrigin: "top right",
  },
  textRight: {
    textAlign: "right",
  },
  button: {
    backgroundColor: "#25d051",
  },
}));

export default useStyles;
